# CalcServ - Сервис решения квадратных уравнений

Веб-сервис для решения квадратных уравнений вида: ax² + bx + c = 0

## 🚀 Статус проекта

🟡 **В разработке** - базовый функционал реализован, требуется доработка

## 📁 Структура проекта
```python
calcserv/
├── src/ # Исходный код
│ ├── quadratic.py ✅ Реализовано: решение квадратных уравнений
│ ├── json_parser.py ✅ Реализовано: парсинг JSON запросов/ответов
│ ├── server.py 🔄 В разработке: FastCGI сервер
│ └── fastcgi_handler.py 🔄 В разработке: обработчик FastCGI
├── tests/ # Тесты
│ ├── test_quadratic.py ✅ Реализовано: тесты для quadratic.py
│ └── test_json_parser.py ✅ Реализовано: тесты для json_parser.py
├── config/ # Конфигурационные файлы
│ ├── nginx.conf 🔄 В разработке
│ └── service.conf 🔄 В разработке
├── requirements.txt 📋 Зависимости Python
├── run_server.py 🔄 В разработке: скрипт запуска сервера
└── start_service.sh 🔄 В разработке
```
## ✅ Реализованный функционал

### 1. Решение квадратных уравнений (`src/quadratic.py`)
```python
from src.quadratic import solve_quadratic

# Пример использования
roots, discriminant = solve_quadratic(1, -5, 6)
# roots = [2.0, 3.0], discriminant = 1.0
```

#### Возможности:
Решение уравнений с действительными корнями
Поддержка комплексных корней
Обработка вырожденных случаев (a=0)
Вычисление дискриминанта

### 2. JSON парсер (`src/json_parser.py`)
```python
from src.json_parser import parse_request, create_response

# Парсинг входящего JSON
json_str = '{"params": {"a": 1, "b": -5, "c": 6}}'
a, b, c = parse_request(json_str)

# Создание JSON ответа
roots = [2.0, 3.0]
discriminant = 1.0
response = create_response(roots, discriminant)
```

#### Возможности:
Валидация входящих данных
Форматирование ответов
Обработка ошибок
Поддержка комплексных чисел в JSON

## 🔧 Установка
### Предварительные требования
Python 3.8+

### Установка
#### Клонирование репозитория
git clone https://github.com/vnuk1/calcserv.git
cd calcserv

#### Установка зависимостей
pip install -r requirements.txt

## 🚀 Запуск
### Запуск тестов

1. Запуск всех тестов
pytest tests/

2. Запуск с подробным выводом
pytest -v tests/

3. Запуск тестов для конкретного модуля
pytest tests/test_quadratic.py -v
pytest tests/test_json_parser.py -v

## Примеры запроса и ответа
Входящий запрос
```json
{
  "params": {
    "a": 1,
    "b": -5,
    "c": 6
  }
}
```
Исходящий ответ
```json
{
  "result": {
    "roots": [2.0, 3.0],
    "discriminant": 1.0,
    "message": "Успех! Два корня"
  },
  "error": null
}
```